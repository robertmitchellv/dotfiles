# My r-profile settings
.libPaths(c("Users/robertmitchellv/Code/R-libs", .libPaths() ) )

options(stringsAsFactors = FALSE)

options(editor = "vim")

options(prompt = "--> ")
options(continue = "···  ")

options(width = 80)

q <- function (save = "no", ...) {
  quit(save = save, ...)
}

